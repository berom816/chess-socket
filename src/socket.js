import socketIOClient from 'socket.io-client';
// let socket = socketIOClient('http://localhost:4001');

// export default socket;
export function emitChangeTurn(){
  // socket.emit('switch turn', 'white');
}
